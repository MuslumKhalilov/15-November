@model List<ProductSize>
<div class="row row-cols-1 row-cols-md-3 g-4">
    @foreach (ProductSize productSize in Model)
    {
        <div class="col">
            <div class="card">
                <img src="~/assets/images/website-images/@productSize.Product.ProductImages.FirstOrDefault(pi=>pi.IsPrimary==true)?.Url" class="card-img-top" alt="@productSize.Product.Name">
                <div class="card-body">
                    <h5 class="card-title">Name: @productSize.Product.Name</h5>
                    <p class="card-text">Price: @productSize.Product.Price</p>
                </div>
            </div>
        </div>
    }
</div>
